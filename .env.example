# Set to '1' or 'true' to build the app with the optional "Connect to database"
# panel enabled (talks to a local companion agent — see docs/plans/db-connect-agent.md).
# Left unset for the GitHub Pages build, so the feature never renders there.
# VITE_ENABLE_DB_AGENT=1
